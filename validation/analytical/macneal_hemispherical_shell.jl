# MacNeal & Harder (1985), Section 9 -- pinched hemispherical shell.
#
# Hemispherical shell, radius R = 10, thickness t = 0.04, E = 6.825e7,
# nu = 0.3. Open at the top (no hole at the pole in the classical MacNeal
# variant; an 18-deg hole is used in Belytschko's variant).
# Free along the equator. Two pairs of opposite radial point unit loads on
# the equator, 90 deg apart.
#
# Reference radial displacement at the loaded point (extrapolated thin-shell
# value used by MacNeal & Harder):
#     radial_disp_at_load = 0.0924

radial_disp_at_load() = 0.0924
